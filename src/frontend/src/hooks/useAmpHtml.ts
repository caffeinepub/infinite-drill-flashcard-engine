// AMP HTML link tags removed — they cause Google Search Console "Crawl issue"
// errors because React SPAs cannot pass AMP validation.
// This file is kept as an empty stub to avoid import errors.
export function useAmpHtml(_path?: string) {
  // intentionally empty — do not add amphtml link tags
}
